let central = $(".connection-center");
let hauteur = window.innerHeight;
if (central != null) {
    central.css('height', hauteur);
}

// j'essayait d'animer le bouton 'inscrivez vous

// let insc = $('#insc');
// console.log(insc);
// insc.click(() => {
//     insc.attr('class', insc.attr('class') + " fadeOut animated");
// });
// console.log(insc);